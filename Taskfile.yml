version: "3"

tasks:
  default:
    desc: "Run default task"
    cmds:
      - task: "install"
      - task: "format"
      - task: "lint"
      - task: "build"

  install:
    desc: "Install dependencies"
    sources:
      - package.json
      - package-lock.json
    cmds:
      - npm ci

  format:
    desc: "Format code"
    deps:
      - install
    cmds:
      - npm run format

  lint:
    desc: "Lint code"
    deps:
      - install
    cmds:
      - npm run lint

  build:
    desc: "Build project"
    deps:
      - install
    cmds:
      - npm run build

  clean:
    desc: "Clean build artifacts"
    cmds:
      - rm -rf dist

  dev:
    desc: "Run development server"
    deps:
      - install
    cmds:
      - npm run dev
